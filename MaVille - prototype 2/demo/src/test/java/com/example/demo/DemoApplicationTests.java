package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Classe de tests pour vérifier le chargement du contexte de l'application Spring Boot.
 * Contient des tests unitaires pour s'assurer que l'application démarre correctement.
 */
@SpringBootTest
class DemoApplicationTests {

    /**
     * Test pour vérifier que le contexte de l'application Spring Boot se charge sans erreur.
     */
    @Test
    void contextLoads() {
    }
}

